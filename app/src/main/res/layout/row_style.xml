<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/activity_vertical_margin">
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/photo"
        style="@style/row_photos"
        tools:src="@android:color/darker_gray"/>
    <LinearLayout
        style="@style/linear_layout_row">
    <TextView
        android:id="@+id/the_title"
        tools:src="Angel Falls, Venezuela"
        style="@style/TextView_row_title"/>
    <TextView
        android:id="@+id/short_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:src="The world's highest waterfall—the 3,212-foot cascades of Angel Falls"/>
    </LinearLayout>
</RelativeLayout>